# Rollback — Release 1.0.02 (Spec 02)

- **Merge commit:** `2d0422f8814b5e5d282a371449c3850a70ef4efb`
- **Tag:** `1.0.02`
- **Fecha:** 2026-09-11
- **Rama:** `spec-02-mapa-real-ruta-frodo` → `master`

## Qué se mergeó

Mapa real de la Tierra Media (mapome, 3200×2400, CC BY 4.0) como asset
estático en `public/mapa/` reemplazando el fondo pixel art (spec 02):
24 waypoints re-ubicados sobre la geografía real con coordenadas en el viewBox
del mapa, campo nuevo `distanceFromStart`, markers circulares y líneas de ruta
suaves, tooltip con tres distancias, crédito CC BY 4.0 en el pie de `/mapa` y
texto descriptivo actualizado.

Además incluye cambios de tooling: docs de Context7 en `.opencode/skills/` y
configuración `opencode.json`.

Tag previo estable: `1.0.01`

## Cómo deshacer

### Opción 1 — revert (recomendada, conserva historia)

```bash
git revert -m 1 2d0422f8814b5e5d282a371449c3850a70ef4efb
```

### Opción 2 — reset duro (descarta la historia del release)

```bash
git checkout master
git reset --hard 1.0.01
```

> ADVERTENCIA: `reset --hard` descarta los cambios irreversiblemente. Usar
> solo si se está seguro de no necesitar el contenido del release.